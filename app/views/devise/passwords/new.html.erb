<h2>Забыли пароль?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :polis, "Номер полиса" %><br />
    <%= f.text_field :polis, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Отправить" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
